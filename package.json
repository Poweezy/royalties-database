{
  "name": "royalties-database",
  "version": "1.0.0",
  "description": "A web application for managing mining royalties.",
  "type": "module",
  "main": "js/app.js",
  "scripts": {
    "dev": "node scripts/inject-env.js && vite",
    "build": "node scripts/inject-env.js && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write .",
    "inject-env": "node scripts/inject-env.js",
    "setup": "cp .env.example .env && node scripts/inject-env.js",
    "security-audit": "node scripts/security-audit.js"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.4.0",
    "chart.js": "^4.4.2",
    "chartjs-adapter-date-fns": "^3.0.0",
    "date-fns": "^3.6.0",
    "leaflet": "^1.9.4",
    "leaflet-geometryutil": "^0.10.1",
    "leaflet.markercluster": "^1.4.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "vite": "^5.1.4"
  }
}
